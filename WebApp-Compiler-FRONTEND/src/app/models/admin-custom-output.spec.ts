import { AdminCustomOutput } from './admin-custom-output';

describe('AdminCustomOutput', () => {
  it('should create an instance', () => {
    expect(new AdminCustomOutput()).toBeTruthy();
  });
});
